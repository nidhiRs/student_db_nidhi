<p id="notice"><%= notice %></p>

<%= link_to 'Home', root_path %>
<h1>Students</h1>
<div class="col-md-12">
<%= form_tag students_path, :method => 'get' do %>
  <p>
    <%= hidden_field_tag :direction, params[:direction] %>
    <%= hidden_field_tag :sort, params[:sort] %>
    <%= text_field_tag :full_name, params[:full_name] %>
    <%= text_field_tag :name, params[:name] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>

<table class="table table-bordered">
  <thead>
    <tr>
      <th><%= sortable "students.full_name", "Name" %></th>
      <th>Address</th>
      <th>Email</th>
      <th><%= sortable "institutions.name", "institution"%></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @students.each do |student| %>
      <tr>
        <td><%= student.full_name %></td>
        <td><%= student.address %></td>
        <td><%= student.email %></td>
        <td><%= student.institution.name %></td>
        <td><%= link_to 'Show', student %></td>
        <td><%= link_to 'Edit', edit_student_path(student) %></td>
        <td><%= link_to 'Destroy', student, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>

<%= link_to 'New Student', new_student_path %>
<br>
<%= link_to 'Pending Students List', pending_students_path %>